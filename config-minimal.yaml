# Minimal configuration - single endpoint for all players
game:
  players: 5
  summary_path: "./runs"
  player_types: ["LLM", "CPU", "CPU", "CPU", "CPU"]

llm:
  default:
    api_key: "your-api-key"
    base_url: "http://localhost:8080/v1/"
